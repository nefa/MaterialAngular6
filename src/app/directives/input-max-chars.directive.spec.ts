import { InputMaxCharsDirective } from './input-max-chars.directive';

describe('InputMaxCharsDirective', () => {
  it('should create an instance', () => {
    const directive = new InputMaxCharsDirective();
    expect(directive).toBeTruthy();
  });
});
